# Sniperkit-Bot
# - Status: analyzed

integrations:

 ant:
  frontend:
   name: "ant"
   route: "ant"

 gradle:
  frontend:
   name: "gradle"
   route: "gradle"

 grunt:
  frontend:
   name: "grunt"
   route: "grunt"
  execution_dir: "/node_build_tools"
  gruntfile_path: "/node_build_tools"

 gulp:
  frontend:
   name: "gulp"
   route: "gulp"
  execution_dir: "/node_build_tools"
  gulpfile_path: "/node_build_tools"